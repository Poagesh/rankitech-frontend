import React from "react";
const AdminConsole = () => (
  <div>
    <h1>Recruiter Console</h1>
    <p>Monitor agent queues, error rates, latency, etc. (coming soon)</p>
  </div>
);
export default AdminConsole;